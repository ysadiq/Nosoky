language: swift

osx_image: xcode10.3

script:
  - xcodebuild -scheme Nosoky -sdk iphonesimulator -destination 'platform=iOS Simulator,name=iPhone XR,OS=latest' build test
  #- sonar-scanner
after_success:
  - bash <(curl -s https://codecov.io/bash)
